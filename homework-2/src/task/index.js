const sumFunction = require('./task1');
const earthInstance = require('./task2');
const timeoutPromise = require('./task3');

module.exports = {
    sumFunction,
    earthInstance,
    timeoutPromise,
};
